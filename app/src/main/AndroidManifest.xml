<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.billionaire_affirmations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/stand"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/stand"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".home"></activity>
        <activity android:name=".fourth_hook_swipe" />
        <activity android:name=".third_hook_swipe" />
        <activity android:name=".second_hook_swipe" />
        <activity android:name=".first_hook_swipe" />
        <activity android:name=".FacePage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>